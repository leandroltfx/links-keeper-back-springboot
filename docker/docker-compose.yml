# run docker-compose --env-file ../.env -p links-keeper up -d

services:
  postgres:
    container_name: links-keeper-container-postgres-docker
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}